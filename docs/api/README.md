# API Documentation

- **[rest_api.md](rest_api.md)** - the full REST API reference (every endpoint, auth/ownership requirements, request/response shape, status values). Source of truth for frontend implementation.
- Interactive/always-current docs: `GET /docs` (Swagger UI) or `GET /openapi.json` on a running instance.
