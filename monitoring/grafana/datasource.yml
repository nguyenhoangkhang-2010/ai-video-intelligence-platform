# Grafana datasource provisioning: auto-registers the local
# Prometheus (see monitoring/prometheus/) so no manual setup is
# needed after `docker compose --profile monitoring up`.
apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false
