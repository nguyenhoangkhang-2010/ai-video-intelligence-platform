/**
 * Video upload lives in services/videos.ts (uploadVideo) alongside
 * the rest of the Video resource it creates — re-exported here so the
 * scaffolded module name/import path is still meaningful.
 */
export { uploadVideo } from "@/services/videos";
export type { UploadVideoOptions } from "@/services/videos";
